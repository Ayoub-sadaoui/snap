kind = "web"
previewPath = "/"
title = "chkopi Login Clone"
description = "A pixel-perfect clone of the chkopi login page, featuring the ghost logo, username/email input, phone number toggle, blue Next button, OR divider, and Continue with Google option."
version = "1.0.0"
id = "artifacts/chkopi-login"
router = "path"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 19487

[services.development]
run = "pnpm --filter @workspace/chkopi-login run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/chkopi-login", "run", "build" ]
serve = "static"
publicDir = "artifacts/chkopi-login/dist/public"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "19487"
BASE_PATH = "/"
